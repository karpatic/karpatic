import{promises as e}from"fs";import r from"path";const t="./sitemap.txt";let a=[];async function processDirectory(t,i=""){if(!(await e.stat(t)).isDirectory())return void console.log("\n\n UNABLE TO PROCESS DIRECTORY: ",t,i);const o=await e.readdir(t);o.filter((e=>".ipynb"===r.extname(e))).map((e=>{a.push(`https://www.cvminigames.com/${i?i+"/":""}${r.parse(e).name}.html`)})),await Promise.all(o.filter((e=>!r.extname(e)&&"notready"!=r.parse(e).name)).map((e=>{processDirectory(r.join(t,r.parse(e).name),r.join(i,r.parse(e).name))})))}(async()=>{await processDirectory("src/ipynb");try{await e.writeFile(t,a.join("\n")+"\n"),console.log("Sitemap file created successfully:",t)}catch(e){console.error(`Error creating or truncating sitemap file: ${t}`,e)}})();